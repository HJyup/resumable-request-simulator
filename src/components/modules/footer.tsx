const Footer = () => (
  <p className="text-center text-xs text-muted-foreground mt-8">
    These requests are not real; this scenario is purely designed to test
    resuming behavior with IndexedDB. To check persistence, you can reload or
    close the page.
  </p>
);

export default Footer;
